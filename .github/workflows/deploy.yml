name: Build and Deploy

on:
  push:
    branches: [master]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      image: ghcr.io/${{ github.repository_owner }}/frontend-platform:${{ steps.sha.outputs.short }}
    steps:
      - uses: actions/checkout@v4

      - name: Compute short SHA
        id: sha
        run: echo "short=${GITHUB_SHA::8}" >> $GITHUB_OUTPUT

      - name: Login to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/frontend-platform:${{ steps.sha.outputs.short }}
            ghcr.io/${{ github.repository_owner }}/frontend-platform:latest

  deploy:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Deploy to K8s
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          command_timeout: 5m
          script: |
            /home/ubuntu/frontend-platform/build-deploy.sh "${{ needs.build.outputs.image }}"

      - name: Copy Keycloak theme to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: "keycloak-theme/"
          target: "/tmp/kypo-theme-ci/"

      - name: Debug kubectl
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            echo "=== which kubectl ==="
            which kubectl 2>/dev/null || echo "kubectl not found"
            which k3s 2>/dev/null || echo "k3s not found"
            which microk8s 2>/dev/null || echo "microk8s not found"
            echo "=== find kubectl ==="
            find /usr /snap /home/ubuntu -name "kubectl" 2>/dev/null || echo "not found"
            echo "=== pods ==="
            sudo kubectl get pod -A 2>/dev/null || \
            sudo k3s kubectl get pod -A 2>/dev/null || \
            sudo microk8s kubectl get pod -A 2>/dev/null || \
            echo "no kubectl available"

      - name: Deploy Keycloak theme
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          command_timeout: 5m
          script: |
            KC_POD=keycloak-0
            KC_NS=crczp
            THEME=kypo-anim
            SRC="/tmp/kypo-theme-ci/keycloak-theme"

            # Auto-detect kubectl
            if which kubectl &>/dev/null; then
              KC="kubectl"
            elif which k3s &>/dev/null; then
              KC="k3s kubectl"
            elif which microk8s &>/dev/null; then
              KC="microk8s kubectl"
            else
              KC="sudo kubectl"
            fi

            echo "Using: $KC"

            $KC exec -n $KC_NS $KC_POD -- \
              mkdir -p /opt/keycloak/themes/$THEME/login/resources/css

            cat $SRC/login/theme.properties | base64 | \
              $KC exec -i -n $KC_NS $KC_POD -- \
              /bin/bash -c "base64 -d > /opt/keycloak/themes/$THEME/login/theme.properties"

            cat $SRC/login/resources/css/login.css | base64 | \
              $KC exec -i -n $KC_NS $KC_POD -- \
              /bin/bash -c "base64 -d > /opt/keycloak/themes/$THEME/login/resources/css/login.css"

            $KC exec -n $KC_NS $KC_POD -- /bin/bash -c "
              /opt/keycloak/bin/kcadm.sh config credentials \
                --server http://localhost:8080/keycloak \
                --realm master \
                --user \$KEYCLOAK_ADMIN \
                --password \$KEYCLOAK_ADMIN_PASSWORD 2>/dev/null && \
              /opt/keycloak/bin/kcadm.sh update realms/crczp \
                -s loginTheme=$THEME 2>/dev/null || true
            "